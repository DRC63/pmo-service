// Vitest global setup (referenced from vite.config.js). Registers jest-dom's DOM
// matchers (toBeInTheDocument, toHaveClass, …) for every test file so each one
// doesn't have to import them.
import '@testing-library/jest-dom/vitest';
